# 🚀 Инструкция по деплою сайта

Этот проект готов к деплою на различные платформы. Ниже описаны варианты размещения сайта на домене.

## 📋 Варианты хостинга

### 1. Vercel (Рекомендуется) ⭐

**Преимущества:**
- ✅ Бесплатный план
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификат включен
- ✅ CDN по всему миру
- ✅ Простая настройка кастомного домена

**Шаги для деплоя:**

1. **Установите Vercel CLI (опционально):**
   ```bash
   npm i -g vercel
   ```

2. **Деплой через CLI:**
   ```bash
   vercel
   ```
   Следуйте инструкциям в терминале.

3. **Или деплой через веб-интерфейс:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите "New Project"
   - Импортируйте ваш репозиторий
   - Vercel автоматически определит настройки из `vercel.json`
   - Нажмите "Deploy"

4. **Настройка кастомного домена:**
   - В настройках проекта → Domains
   - Добавьте ваш домен
   - Следуйте инструкциям по настройке DNS

**Конфигурация уже готова:** `vercel.json`

---

### 2. Netlify

**Преимущества:**
- ✅ Бесплатный план
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификат включен
- ✅ Простая настройка

**Шаги для деплоя:**

1. **Деплой через CLI:**
   ```bash
   npm i -g netlify-cli
   netlify deploy --prod
   ```

2. **Или деплой через веб-интерфейс:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Войдите через GitHub
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите репозиторий
   - Настройки:
     - Build command: `npm run build`
     - Publish directory: `dist`
   - Нажмите "Deploy site"

**Конфигурация уже готова:** `netlify.toml`

---

### 3. GitHub Pages

**Преимущества:**
- ✅ Полностью бесплатно
- ✅ Интеграция с GitHub
- ✅ Автоматический деплой через GitHub Actions

**Шаги для деплоя:**

1. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages
   - Source: GitHub Actions

2. **Автоматический деплой:**
   - Файл `.github/workflows/deploy.yml` уже настроен
   - При каждом push в `main` ветку сайт автоматически обновится

3. **Настройка кастомного домена:**
   - В Settings → Pages добавьте ваш домен
   - Создайте файл `CNAME` в папке `public` с вашим доменом

**Конфигурация уже готова:** `.github/workflows/deploy.yml`

---

### 4. Cloudflare Pages

**Преимущества:**
- ✅ Бесплатный план
- ✅ Отличная производительность
- ✅ Интеграция с Cloudflare

**Шаги для деплоя:**

1. Зайдите на [pages.cloudflare.com](https://pages.cloudflare.com)
2. Войдите через GitHub
3. Выберите репозиторий
4. Настройки:
   - Build command: `npm run build`
   - Build output directory: `dist`
5. Нажмите "Save and Deploy"

---

## 🔧 Подготовка к деплою

Перед деплоем убедитесь, что:

1. **Проект собран:**
   ```bash
   npm run build
   ```

2. **Все изменения закоммичены:**
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push
   ```

3. **Проверьте, что `dist` папка содержит все необходимые файлы**

---

## 🌐 Настройка кастомного домена

### Для Vercel/Netlify:

1. **Добавьте домен в настройках проекта**
2. **Настройте DNS записи у вашего регистратора домена:**

   **Для Vercel:**
   - A запись: `76.76.21.21` (или CNAME на `cname.vercel-dns.com`)

   **Для Netlify:**
   - CNAME запись на `your-site.netlify.app`

3. **Подождите распространения DNS (обычно 5-60 минут)**

### Для GitHub Pages:

1. Создайте файл `public/CNAME` с вашим доменом:
   ```
   yourdomain.com
   ```

2. Настройте DNS:
   - A записи: `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`
   - Или CNAME на `your-username.github.io`

---

## 📝 Проверка после деплоя

После деплоя проверьте:

- ✅ Сайт открывается по URL
- ✅ Все изображения загружаются
- ✅ Навигация работает
- ✅ Формы работают (если есть)
- ✅ SSL сертификат активен (HTTPS)

---

## 🚨 Решение проблем

### Сайт не открывается:
- Проверьте, что сборка прошла успешно
- Убедитесь, что `dist` папка содержит `index.html`
- Проверьте логи деплоя в панели хостинга

### Изображения не загружаются:
- Проверьте пути к изображениям (должны быть относительными)
- Убедитесь, что папка `public` содержит все ресурсы

### 404 ошибки:
- Убедитесь, что настроены правильные rewrites/redirects
- Для SPA все маршруты должны вести на `index.html`

---

## 💡 Рекомендации

**Для продакшена рекомендуется:**
1. ✅ Vercel - лучший баланс простоты и производительности
2. ✅ Netlify - хорошая альтернатива
3. ✅ GitHub Pages - если проект уже на GitHub

**Для максимальной производительности:**
- Используйте CDN (включен в Vercel/Netlify)
- Включите кеширование (уже настроено в конфигах)
- Оптимизируйте изображения (уже сделано)

---

## 📞 Поддержка

Если возникли проблемы с деплоем, проверьте:
- Логи сборки в панели хостинга
- Конфигурационные файлы (`vercel.json`, `netlify.toml`)
- Что все зависимости установлены (`npm install`)
