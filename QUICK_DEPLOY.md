# ⚡ Быстрый деплой на Vercel

## Самый простой способ (5 минут):

### 1. Через веб-интерфейс Vercel:

1. **Зайдите на [vercel.com](https://vercel.com)**
2. **Войдите через GitHub** (или создайте аккаунт)
3. **Нажмите "Add New..." → "Project"**
4. **Импортируйте ваш репозиторий:**
   - Найдите `elizaveta-portfolio` в списке
   - Нажмите "Import"
5. **Настройки (уже предустановлены):**
   - Framework Preset: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`
6. **Нажмите "Deploy"**

✅ **Готово!** Через 1-2 минуты ваш сайт будет доступен по адресу:
`https://elizaveta-portfolio.vercel.app`

---

### 2. Подключение кастомного домена:

1. **В настройках проекта Vercel:**
   - Перейдите в Settings → Domains
   - Нажмите "Add Domain"
   - Введите ваш домен (например: `elizaveta-vakalova.com`)

2. **Настройте DNS у регистратора домена:**
   - Добавьте CNAME запись:
     - Имя: `@` или `www`
     - Значение: `cname.vercel-dns.com`
   - Или A записи (если CNAME не поддерживается):
     - `76.76.21.21`

3. **Подождите 5-60 минут** для распространения DNS

4. **Vercel автоматически выдаст SSL сертификат**

---

### 3. Автоматический деплой:

После первого деплоя:
- ✅ Каждый push в `main` ветку автоматически обновит сайт
- ✅ Pull requests получат preview URL
- ✅ Все бесплатно на плане Hobby

---

## Альтернатива: Netlify

1. Зайдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub
3. "Add new site" → "Import an existing project"
4. Выберите репозиторий
5. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. "Deploy site"

---

## Проверка после деплоя:

- [ ] Сайт открывается
- [ ] Все изображения загружаются
- [ ] Навигация работает
- [ ] Форма контактов работает
- [ ] HTTPS активен (замочек в браузере)

---

## Нужна помощь?

Если что-то не работает:
1. Проверьте логи деплоя в панели Vercel/Netlify
2. Убедитесь, что `package.json` содержит скрипт `build`
3. Проверьте, что все зависимости в `package.json`

**Готово к деплою! 🚀**
